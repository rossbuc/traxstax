{% extends 'users/user.jinja' %} {% block content_inner %} {{ super() }}
    <h2>Tracklist are gonna go here!</h2>
    {% for tracklist in tracklists %}
        <div>
            <a href="/users/1/tracklists/ {{ tracklist.id }}">{{ tracklist.name }}</a>
        </div>
    {% endfor %}
    <form action="/users/1/tracklists/new">
        <button type="submit">New Tracklist</button>
    </form>
    
{% endblock %}