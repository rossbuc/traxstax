
{% extends './base.jinja' %} {% block content %}
    <section class="hero-user">
        <div class="user--profile_image__container"><img src="{{ user.profile_image }}" alt="user profile picture" class="profile-image"></div>
        <ul class="user-details">
            <li><h2 class="username">{{ user.username }}</h2></li>
            <li>Bio</li>
        </ul>
    </section>
    <div class="content-user">
        <nav class="nav-user">
            <ul>
                <li><a href="/traxstax/{{ user.id }}/uploads">Uploads</a></li>
                <li><a href="/traxstax/{{ user.id }}/tracklists">Tracklists</a></li>
                <li>Friends</li>
                <li>Settings</li>
            </ul>
        </nav>
        <div class="display-user">
            {% block content_inner %}
                
            {% endblock %}
        </div>
    </div>

{% endblock content %}
